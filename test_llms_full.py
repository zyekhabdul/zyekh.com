import os
import glob
from bs4 import BeautifulSoup # Maybe use regex instead if bs4 not available

# Actually we can just read the HTML files or Markdown source files?
# Where does the blog content come from?
